# B2G — Happy Path

See `spine.yaml`. Government raises procurement (internally approved) → selects from catalog → issues SP/SPK with DIPA reference → delivery → BAST signed → SP2D payment less PPh22.
